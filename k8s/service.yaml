apiVersion: v1
kind: Service
metadata:
  name: agent-backend
  namespace: agent-demo
spec:
  selector:
    app: agent-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: LoadBalancer
